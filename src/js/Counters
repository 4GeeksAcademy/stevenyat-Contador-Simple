const conterSimple = let counterSimple = 0;
setInterval(() => {
    counterSimple += 1;
    root.render(
  <React.StrictMode>
    <SecondsCounter seconds = {counterSimple}/>
  </React.StrictMode>,
)

}, 1000);
expor

export default conterSimple 